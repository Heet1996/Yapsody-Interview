class Events
{
    constructor(events)
    {
        this.title1=events.title1;
        this.title2=events.title2;
        this.location=events.location;
        this.location_type=events.location_type;
        this.performance=events.performance;
    }
}

module.exports={Events};